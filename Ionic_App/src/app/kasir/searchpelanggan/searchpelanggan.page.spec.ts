import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SearchpelangganPage } from './searchpelanggan.page';

describe('SearchpelangganPage', () => {
  let component: SearchpelangganPage;
  let fixture: ComponentFixture<SearchpelangganPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SearchpelangganPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
