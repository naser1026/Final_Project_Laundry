import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdatepelangganPage } from './updatepelanggan.page';

describe('UpdatepelangganPage', () => {
  let component: UpdatepelangganPage;
  let fixture: ComponentFixture<UpdatepelangganPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdatepelangganPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
