import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PilihpelangganPage } from './pilihpelanggan.page';

describe('PilihpelangganPage', () => {
  let component: PilihpelangganPage;
  let fixture: ComponentFixture<PilihpelangganPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PilihpelangganPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
